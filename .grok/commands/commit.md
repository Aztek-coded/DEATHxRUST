# Commit

Follow the project skill at `.grok/skills/commit/SKILL.md`.

Summarize: stage intentional changes, write a clear commit message, commit. Do **not** push unless the user explicitly asked to push.
